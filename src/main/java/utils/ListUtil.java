package utils;

import java.util.Comparator;
import java.util.List;

public class ListUtil {

    public static void sorted(List list){

//        list = list.stream().sorted(Comparator.comparing(字段).reversed()).collect(Collectors.toList());
    }
}
